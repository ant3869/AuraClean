<UserControl x:Class="AuraClean.Views.MemoryBoostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:AuraClean.Converters"
             xmlns:helpers="clr-namespace:AuraClean.Helpers">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24"
                    helpers:AnimationHelper.AnimateOnVisible="True"
                    helpers:AnimationHelper.StaggerIndex="0">
            <TextBlock FontSize="26" FontWeight="Light"
                       Foreground="{DynamicResource AuraTextBright}">
                <Run Text="Memory " /><Run Text="Optimizer" FontWeight="SemiBold" />
            </TextBlock>
            <TextBlock Text="Free up memory held by background processes to speed up your system"
                       Foreground="{DynamicResource AuraTextSecondary}"
                       FontSize="13"
                       Margin="0,4,0,0" />
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- RAM Usage Gauge -->
            <Border Grid.Column="0" Style="{StaticResource AuraGlassCard}"
                    CornerRadius="16" Padding="24">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="RAM Usage" FontSize="18" FontWeight="SemiBold"
                               Foreground="{DynamicResource AuraTextPrimary}"
                               HorizontalAlignment="Center" Margin="0,0,0,16" />

                    <!-- Circular gauge -->
                    <Grid Width="200" Height="200" HorizontalAlignment="Center">
                        <Ellipse Width="180" Height="180" Stroke="{DynamicResource AuraBorderSubtle}" StrokeThickness="14" Fill="Transparent" />
                        <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     Width="180" Height="180"
                                     Value="{Binding UsagePercent}" Minimum="0" Maximum="100"
                                     Foreground="{StaticResource AuraAccentPurple}"
                                     IsIndeterminate="False" />
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding UsagePercent, StringFormat={}{0:F0}%}"
                                       FontSize="36" FontWeight="Bold"
                                       Foreground="{StaticResource AuraAccentPurple}"
                                       HorizontalAlignment="Center" />
                            <TextBlock Text="in use"
                                       FontSize="13" Foreground="{DynamicResource AuraTextSecondary}"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Grid>

                    <!-- RAM Stats -->
                    <StackPanel Margin="0,20,0,0">
                        <Grid Margin="0,4">
                            <TextBlock Text="Total" Foreground="{DynamicResource AuraTextSecondary}" />
                            <TextBlock Text="{Binding FormattedTotal}" HorizontalAlignment="Right"
                                       Foreground="{DynamicResource AuraTextPrimary}" FontWeight="SemiBold" />
                        </Grid>
                        <Grid Margin="0,4">
                            <TextBlock Text="Used" Foreground="{DynamicResource AuraTextSecondary}" />
                            <TextBlock Text="{Binding FormattedUsed}" HorizontalAlignment="Right"
                                       Foreground="{StaticResource AuraWarning}" FontWeight="SemiBold" />
                        </Grid>
                        <Grid Margin="0,4">
                            <TextBlock Text="Available" Foreground="{DynamicResource AuraTextSecondary}" />
                            <TextBlock Text="{Binding FormattedAvailable}" HorizontalAlignment="Right"
                                       Foreground="{StaticResource AuraSuccess}" FontWeight="SemiBold" />
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Boost Results & Actions -->
            <Border Grid.Column="2" Style="{StaticResource AuraGlassCard}"
                    CornerRadius="16" Padding="24">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Boost Results" FontSize="18" FontWeight="SemiBold"
                               Foreground="{DynamicResource AuraTextPrimary}"
                               HorizontalAlignment="Center" Margin="0,0,0,20" />

                    <!-- Results (shown after boost) -->
                    <StackPanel Visibility="{Binding HasBoostResult, Converter={StaticResource BoolToVis}}"
                                Margin="0,0,0,20">
                        <Border Style="{StaticResource AuraGlassCardCompact}"
                                Padding="16" Margin="0,0,0,8">
                            <Grid>
                                <StackPanel>
                                    <TextBlock Text="Memory Freed" FontSize="12"
                                               Foreground="{DynamicResource AuraTextSecondary}" />
                                    <TextBlock Text="{Binding FormattedFreed}" FontSize="28" FontWeight="Bold"
                                               Foreground="{StaticResource AuraSuccess}" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid Margin="0,4">
                            <TextBlock Text="Processes trimmed" Foreground="{DynamicResource AuraTextSecondary}" />
                            <TextBlock Text="{Binding LastTrimmedCount}" HorizontalAlignment="Right"
                                       Foreground="{DynamicResource AuraTextPrimary}" FontWeight="SemiBold" />
                        </Grid>
                        <Grid Margin="0,4">
                            <TextBlock Text="Standby list purged" Foreground="{DynamicResource AuraTextSecondary}" />
                            <TextBlock HorizontalAlignment="Right"
                                       Foreground="{DynamicResource AuraTextPrimary}" FontWeight="SemiBold">
                                <TextBlock.Text>
                                    <Binding Path="StandbyPurged" StringFormat="{}{0}" />
                                </TextBlock.Text>
                            </TextBlock>
                        </Grid>
                    </StackPanel>

                    <!-- Dry Run Toggle -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,16">
                        <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                      AutomationProperties.Name="Dry Run"
                                      IsChecked="{Binding IsDryRun}" Margin="0,0,8,0" />
                        <TextBlock Text="Dry Run (Preview Only)"
                                   Foreground="{DynamicResource AuraTextSecondary}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- Boost Button -->
                    <Button Content="{materialDesign:PackIcon Kind=RocketLaunch}"
                            Style="{StaticResource MaterialDesignFloatingActionButton}"
                            Width="72" Height="72"
                            Background="{StaticResource AuraAccentPurple}"
                            Foreground="White" BorderThickness="0"
                            Command="{Binding BoostMemoryCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                            AutomationProperties.Name="Boost Memory"
                            HorizontalAlignment="Center"
                            ToolTip="One-Click Boost">
                        <Button.Effect>
                            <DropShadowEffect Color="{StaticResource AuraVioletColor}" BlurRadius="24" ShadowDepth="0" Opacity="0.5" />
                        </Button.Effect>
                    </Button>
                    <TextBlock Text="BOOST" FontSize="14" FontWeight="Bold"
                               Foreground="{StaticResource AuraAccentPurple}"
                               HorizontalAlignment="Center" Margin="0,8,0,0" />

                    <!-- Refresh -->
                    <Button Content="Refresh Stats"
                            Style="{StaticResource AuraOutlinedButton}"
                            Padding="16,8"
                            Command="{Binding RefreshStatsCommand}"
                            HorizontalAlignment="Center" Margin="0,16,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}"
                   AutomationProperties.LiveSetting="Polite"
                   Foreground="{DynamicResource AuraTextSecondary}"
                   FontSize="13" Margin="0,16,0,0" />
    </Grid>
</UserControl>
